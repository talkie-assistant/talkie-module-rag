# RAG module

Document ingest, embedding (Ollama), and retrieval for LLM context. See the [project wiki — Module RAG](https://github.com/talkie-assistant/talkie-core/wiki/Module-RAG) for full documentation.

- **RAG mode:** Press **H** or **h** to show this help.
- **Documents:** Use the Documents panel to upload files and manage sources.

Override config in root `config.yaml` or `config.user.yaml`.
